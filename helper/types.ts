export type ToastTypes = {
    pending: string,
    success: string,
    error: string,
}
export type ContractMetaData = {
    name: string | null,
    symbol: string | null,
    contractAddress: string | null
}
